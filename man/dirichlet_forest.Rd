% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dirichlet_forest.R
\name{dirichlet_forest}
\alias{dirichlet_forest}
\title{Dirichlet Forest}
\usage{
dirichlet_forest(
  X,
  Y,
  B = 100,
  d_max = 10,
  n_min = 5,
  m_try = -1,
  seed = 123,
  method = "mom"
)
}
\arguments{
\item{X}{Numeric matrix of predictors}

\item{Y}{Numeric matrix of compositional response variables (rows sum to 1)}

\item{B}{Integer, number of trees in the forest (default: 100)}

\item{d_max}{Integer, maximum depth of trees (default: 10)}

\item{n_min}{Integer, minimum number of samples in terminal nodes (default: 5)}

\item{m_try}{Integer, number of features to try at each split (default: sqrt(ncol(X)))}

\item{seed}{Integer, random seed (default: 123)}

\item{method}{String, estimation method ("mle" or "mom", default: "mle")}
}
\value{
A list containing the trained forest model
}
\description{
Build a Dirichlet Forest for compositional data
}
\examples{
\dontrun{
# Generate sample data
X <- matrix(rnorm(100*5), 100, 5)
Y <- matrix(runif(100*3), 100, 3)
Y <- Y / rowSums(Y)  # Make compositional

# Train forest
forest <- dirichlet_forest(X, Y, B = 50)

# Make predictions
predictions <- predict_dirichlet_forest(forest, X[1:10, ])
}
}
